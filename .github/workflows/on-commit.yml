name: on-commit

on:
  push:
    branches:
      - develop

jobs:
  my-simple-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        #- run: echo "Unbelievable success!"

        
