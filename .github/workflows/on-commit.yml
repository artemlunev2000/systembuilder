name: on-commit

on:
  push:
    branches:
      - master

jobs:
  my-simple-job:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        #- run: echo "Unbelievable success!"

        
